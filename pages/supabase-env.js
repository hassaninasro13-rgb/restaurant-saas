/* Placeholder: overwritten by `npm run build` (scripts/inject-env.mjs) when env vars are set. */
(function () {
  'use strict';
  try {
    globalThis.__ONETAP_SUPABASE__ = null;
  } catch (e) { /* ignore */ }
})();
