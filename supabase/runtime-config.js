/**
 * Supabase URL + anon key (safe in browser with RLS).
 * For production (Vercel, CI): set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY — `npm run build` overwrites this file.
 * For local dev: replace the placeholders below or run the build with those env vars.
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
